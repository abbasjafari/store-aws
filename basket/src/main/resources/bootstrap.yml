spring:
  application:
    name: basket
  cloud:
      discovery:
        enabled: true
      config:
        discovery:
          enabled: true
eureka:
  client:
    server-url:
      defaultZone: http://localhost:8761/eureka