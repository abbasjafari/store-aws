server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alikarimi8765/spring-cloud-sample.git
          search-paths:
            - config*
  application:
    name: configserver

eureka:
  client:
    server-url:
      defaultZone: http://localhost:8761/eureka